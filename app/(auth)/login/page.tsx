import { LoginForm } from "@/components/forms/login-form"

export default function LoginPage() {
  return (
    <div className="min-h-screen w-full bg-muted/30 flex items-center justify-center px-4 py-8">
      <LoginForm />
    </div>
  )
}
